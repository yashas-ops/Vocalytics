# TODO Refinement Pass 1 (CSS-only)

## Goals
- Preserve baseline tests and all selector contracts.
- Only adjust CSS values/additive rules in:
  - `assets/styles.css`
  - `assets/light.css`

## Steps
1. Table typography/readability integration (DataFrame grid/header/rows).
2. Card/surface padding + subtle glass treatment (only on existing `.surface-hero` / `.executive-summary` / card containers).
3. Background depth layering improvements on `.stApp` (dark + light).
4. Hover/motion timing standardization across key interactive elements.
5. Mirror all changes in light mode.
6. Update `TODO.md` checkboxes for completed items.

